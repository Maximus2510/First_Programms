const kelvin = 0;
// константа в кельвинах
let celsius = kelvin - 273;
// кельвины больше цельсия на 273
let fahrengate = celsius * (9 / 5) + 32;
//формула для перевода градусов цельсия в фаренгейты
fahrengate = Math.floor(fahrengate);
//
console.log(`The temperature is ${fahrengate} degrees Fahrenheit.`);
let newton = celsius*(33/100);
//формула для перевода градусов цельсия в фаренгейты
fahrengate = Math.floor(newton);
console.log(`The temperature is ${newton} degrees Newton.`);